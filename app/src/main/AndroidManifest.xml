<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.shadowsoft.adminEnd">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <permission
        android:name="in.shadowsoft.adminEnd.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.barcodelibrary.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/admin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/admin"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Homestay"
            android:label="@string/title_activity_homestay"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Sell"
            android:label="@string/title_activity_sell"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Sign_up"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".upload_product"
            android:label="@string/title_activity_upload_product"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Speech" />
        <activity android:name=".voice" />
        <activity
            android:name=".Tribal_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".get" />
        <activity android:name=".Database" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Homestay_location"
            android:label="@string/title_activity_homestay_location"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".homestay_upload"
            android:label="@string/title_activity_homestay_upload"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".destinations"
            android:label="@string/title_activity_destinations"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Destination_location"
            android:label="@string/title_activity_destination_location"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".event"
            android:label="@string/title_activity_event"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".guide"
            android:label="@string/title_activity_guide"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".event_upload"
            android:label="@string/title_activity_event_upload"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".event_map"
            android:label="@string/title_activity_event_map"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".food_outlet"
            android:label="@string/title_activity_food_outlet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".food_outlet_upload"
            android:label="@string/title_activity_food_outlet_upload"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".food_outlet_location"
            android:label="@string/title_activity_food_outlet_location"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>